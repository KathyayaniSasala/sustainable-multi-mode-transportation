<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css"/>
    <script src="https://code.jquery.com/jquery-2.2.0.min.js"></script>
    <!-- <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"> -->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.3.1/css/all.css">
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
    <style>
.navbar {
      display: flex !important;
      background-color: #333 !important;
      padding: 10px !important;
  }
  .a{
    font-size: 25px
  }
  
  .navbar a {
      color: white !important;
      text-decoration: none !important;
      padding: 10px !important;
      margin-right: 10px !important;
  }
  
  .navbar a:hover {
      background-color: #555 !important;
      color: white !important;
  }

  .hand-pointer {
      cursor: pointer;
    }

    .dropdown {
      margin-left: auto;
    }

    .dropdown button {
      border-radius: 60%;
      font-size: 1.2rem;
      padding: 15px 10px;
    }

    .dropdown-menu {
      right: 4px;
      left: auto;
    }

    </style>
</head>
<body>
    <nav class="navbar">
        <a style="padding-left:300px">Sustainable Multi-Mode Transportation</a>
        <a href="/user-welcome">Home</a>
        <a href="/user-weather">Weather</a>
        <a href="/search-history">History</a>
    
        <% if (user && user.role === 'admin') { %>
            <a href="/welcome">Admin</a>
        <% } %>
        
        <!-- User dropdown menu -->
        <div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle" type="button" id="userDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fas fa-user fa-xl"></i>
              </button>
          <div class="dropdown-menu" style="margin-right: 40px;" aria-labelledby="userDropdown">
            
            <p class="dropdown-item"><%= user.user_name %></p>
            <p class="dropdown-item"><%= user.email %></p>
            <p class="dropdown-item"><%= user.mob_no %></p>
            <div class="dropdown-divider"></div>
            <p class="hand-pointer" id="logoutBtn" class="dropdown-item">Logout</p>
          </div>
        </div>
    
        <!-- Logout button script -->
        <script>
          document.getElementById('logoutBtn').addEventListener('click', () => {
            window.location.href = '/logout';
          });
        </script>
      </nav>
